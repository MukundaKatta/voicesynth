from setuptools import setup, find_packages
setup(name="voicesynth", version="0.1.0", description="Open Source Text-to-Speech with voice cloning and emotion control",
      packages=find_packages(), python_requires=">=3.9")
